/*1. Crie um programa que copie para um vetor os elementos de outro em ordem crescente.

2.Implemente um programa que recebe dois vetores de números inteiros, A e B,
com capacidade para 5 números cada. Em seguida o programa deve

intercalar os vetores, gerando um vetor C. O vetor resultante C terá
tamanho 10, e seus elementos são formados pelos elementos de A e B
alternados, começando pelo vetor A. Por exemplo: para A = {1, 2, 3, 4, 5} e
B = {10, 20, 30, 40, 50}, a intercalação de A e B resulta em C = {1, 10, 2, 20,
3, 30, 4, 40, 5, 50}.

3. Implemente um programa que recebe um valor N correspondente ao tamanho de um vetor de inteiros com no máximo 30 elementos (o programa

deve verificar se o valor de N é válido). Em seguida o programa deve
receber e armazenar, no vetor, N números inteiros em ordem não decrescente. Feito isso, o programa deve:

a) calcular e imprimir, para cada número do vetor, quantas vezes ele se repete.

b) criar um segundo vetor que vai receber os valores do primeiro vetor, mas sem valores repetidos.*/
